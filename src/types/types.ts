export type SingleOffer = {
        id: number,
        class: string,
        image: string,
        price: string,
        title: string,
        type: string
}

export type OfferData = SingleOffer[]
